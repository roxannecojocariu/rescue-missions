<div class='index'>
  Questions:

  <ul></br>
    <% @questions.each do |question| %>
      <p><%= link_to question.title, question_path(question)%>
    <% end %>
  </ul></br></br>

  <%= link_to 'New Question', new_question_path, class: 'links' %>
</div>
